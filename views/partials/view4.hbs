<div id="view4" class="container-fluid pb-5">
    {{#if viewFour.allOverlap}}
    <div class="row pt-3 pb-4">
        <div class="bg card col-md-6 mx-auto">
            <div class="card-body">
                <h5 class="card-title">No hidden posts!</h5>
                <p class="card-text">There seem to be no hidden posts when you should currently view the first
                    {{viewFour.allPosts.length}} posts on your Instagram! Probably you've frequently opened Instagram
                    the past hours. Because you've seen all posts ("you're all up to date"), recency will become the
                    most important factor for the algorithm. This is why the non-algorithm list has the same ranking as
                    the algortihm based list, which means these two are equal.</p>
            </div>
        </div>
    </div>
    {{/if}}
    <div class="row pt-4">
        <div class="col-md-6 mx-auto">
            <div class="card mb-4">
                <div class="card-header" id="overlap">
                    <h5 class="mb-0">
                        <button class="btn btn-link text-secondary" type="button" data-toggle="collapse"
                            data-target="#collapseOverlap" aria-expanded="true" aria-controls="collapseOverlap">
                            Based on all <span id="totalTopPosts">{{viewFour.totalCountTopPosts}}</span> posts in your
                            feed,
                            {{viewFour.beginOverlap.topPosts.length}} posts also appear in your feed without algorithm
                            (i.e. chronologically). These {{viewFour.beginOverlap.topPosts.length}} are therefore not
                            'hided' by the algorithm in case you only see the first {{viewFour.totalCountTopPosts}}
                            posts.</br>Click here to view them.
                            <i class="fas fa-caret-down"></i>
                        </button>
                    </h5>
                </div>

                <div id="collapseOverlap" class="collapse" aria-labelledby="overlap">
                    <div class="card-body">
                        {{#times viewFour.beginOverlap.topPosts.length}}
                        <div class="row">
                            {{!-- {{#with (lookup @root.viewFour.beginOverlap.allPosts @this)}}
                            {{> nodeMeta node.meta }}
                            {{/with}} --}}
                            {{#with (lookup @root.viewFour.beginOverlap.allPosts @this)}}
                            {{> nodeOverlap node }}
                            {{/with}}
                            {{#with (lookup @root.viewFour.beginOverlap.topPosts @this)}}
                            {{> nodeOverlap node }}
                            {{/with}}
                        </div>
                        {{/times}}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="offset-md-2 col-md-4 pt-5 pb-1">
            <h5 class="text-right">
                Your feed without algortihm <i class="fas fa-sort-numeric-down"></i>
            </h5>
        </div>
        <div class="col-md-4 pt-5 pb-1">
            <h5>
                Your feed with algorithm <i class="fas fa-random"></i>
                <br>(identical to the app)
            </h5>
        </div>
    </div>
    {{!-- show the overlap but collapsed --}}


    {{#times viewFour.allPosts.length}}
    <div class="row">
        {{#with (lookup @root.viewFour.allPosts @this)}}
        {{> nodeMetaLeft node.meta }}
        {{/with}}
        {{#with (lookup @root.viewFour.allPosts @this)}}
        {{> node node }}
        {{/with}}
        {{#with (lookup @root.viewFour.topPosts @this)}}
        {{> node node }}
        {{/with}}
    </div>
    {{/times}}

</div>